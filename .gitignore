# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.python-version

# Local config / secrets — keep config.yaml.example committed (TBD), real config
# may carry vendor IDs and api keys; allow committing the default template.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
*.log

# Runtime registry — per-machine printer/terminal assignments
printers.json
terminals.json

# CLI runtime — PID of the detached server (written by `python cli.py start`)
bhm.pid

# Per-install identifier — generated at first uplink start, NOT a secret
# but unique per machine and shouldn't be committed.
install_id.txt

# Артефакти локальної збірки PyInstaller. Потрапили в коміт 709eef9 разом із
# 28-мегабайтним бінарником — саме те, чого в репо бути не має.
dist/
build/
